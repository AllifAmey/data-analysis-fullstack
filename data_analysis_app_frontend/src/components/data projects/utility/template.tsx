import React from "react";

type props = {
  example: string;
};

const HomePage = ({ example }: props) => {
  /*
  
  
  */

  //styles

  return (
    <>
      <div>{example}</div>
    </>
  );
};

export default HomePage;
